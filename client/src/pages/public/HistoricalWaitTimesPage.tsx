import { PublicLayout } from '../../components/layout/PublicLayout';
import { Card } from '../../components/ui/Card';

export function HistoricalWaitTimesPage() {
  return (
    <PublicLayout>
      <Card>
        <p className="text-sm text-slate-500">HistoricalWaitTimesPage placeholder for chart summaries and best times to visit.</p>
      </Card>
    </PublicLayout>
  );
}
